# setChecker

### Steps to install
1. Clone this repository
2. run cd setChecker
3. run npm install

## Instructions on using the code

To generate a random array of sets that contain between 700 - 1500 15-digit numbers
* run node generateRandomSet.js

To verify the sets in randomSet.json
* run node verify.js